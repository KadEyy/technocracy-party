import styled from 'styled-components';

export const StyledExternal = styled.a`
    text-decoration: none;
    color: var(--third);
    cursor: pointer;
    &:active, &:visited{
        color: var(--third);
    }
`;